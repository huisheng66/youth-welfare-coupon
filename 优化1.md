# 工程优化计划（优化1）

继 [`优化.md`](优化.md) 的安全硬化之后，本文聚焦**工程债、性能、可维护性、可演进性**四个维度，按优先级排出可落地的优化项。
安全项不再重复（仍以 `优化.md` 为准）；本文与安全文档互补，互不覆盖。

自动化回归：`backend/tests/test_security_hardening.py`、`backend/scripts/security_audit*.py`。

---

## 总览

| # | 问题 | 维度 | 风险 | 建议优先级 | 预估工作量 | 状态 |
|---|------|------|------|------------|------------|------|
| 1 | 缺 Alembic 迁移，靠 `create_all` + 手写 ALTER | 可演进 | 多机部署/回滚失控、字段升级累积出错 | **P0** | 1–1.5 天 | 完成 |
| 2 | JWT 存 `localStorage`，XSS 即丢 token | 安全/可演进 | 一次 XSS 全量失陷 | **P0** | 1.5–2 天 | 完成 |
| 3 | 列表接口 N+1 查询（`coupon_to_out` 等） | 性能 | 分页大时 QPS 瀑布 | **P0** | 0.5–1 天 | 完成 |
| 4 | `database.py` 模块级建 engine | 可测试 | 测试切配置难、生命周期不可控 | **P1** | 0.5 天 | 完成 |
| 5 | `@app.on_event("startup")` 已弃用 | 可维护 | FastAPI 升级踩坑 | **P1** | 0.5 天 | 完成 |
| 6 | `deploy/` 脚本爆炸，一次性补丁未归档 | 可维护 | 新人误执行、检索成本高 | **P1** | 0.5 天 | 完成 |
| 7 | 单元/集成测试覆盖薄（仅 2 个安全测试文件） | 质量 | 重构无回归网 | **P1** | 2–3 天 | 完成 |
| 8 | `tools/nuclei-templates/` 未进 CI | 安全运维 | 自家渗透模板沦为手工工具 | **P2** | 0.5 天 | 完成 |
| 9 | 前端无路由懒加载、无构建产物分析 | 性能 | 首屏体积偏大 | **P2** | 0.5 天 | 完成 |
| 10 | 日志结构化缺失（`logging.getLogger` 裸用） | 可运维 | 生产排障/聚合难 | **P2** | 1 天 | 完成 |

**原则**：先堵影响生产稳定性的三块（迁移 / Token / N+1）→ 再清工程债（engine 生命周期 / lifespan / 部署脚本 / 测试网）→ 最后补运维与前端性能。

---

## 分项计划

### 1. 接入 Alembic 迁移（P0）

**目标**：以版本化迁移替代 `create_all` + `migrate.py` 手写补丁。

**现状**：
- `backend/app/main.py` 启动时 `Base.metadata.create_all(bind=engine)` + `ensure_schema(engine)`。
- `backend/app/core/migrate.py` 手写 `_add_column_if_missing` / `_migrate_hours_to_decimal` 等 ALTER 补丁，已出现 INTEGER→DECIMAL 的就地升级逻辑——这类补丁累积下去会失控。
- README 自己也写了"生产建议再接入 Alembic"。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 初始化 Alembic | `alembic init backend/alembic`，`alembic.ini` 指向 `backend/alembic` | 新增目录 |
| 共享 engine | `env.py` 复用 `app.core.database.engine` 与 `Base.metadata` | `backend/alembic/env.py` |
| 首个基线迁移 | `alembic revision --autogenerate -m "baseline"` 对齐当前 schema | `backend/alembic/versions/xxx_baseline.py` |
| 启动策略 | 开发：`create_all`（兼容空库）；生产：`alembic upgrade head` | `main.py` `on_startup` |
| 回收手写补丁 | `migrate.py` 的 `_migrate_hours_to_decimal` 等转为正式迁移 | 删除或保留为 no-op |
| 部署集成 | `install-ubuntu.sh` / `welfare-api.service` 在启动前跑 `alembic upgrade head` | `deploy/` |

**验收**：
- 新机器空库 → `alembic upgrade head` 后 schema 与 `create_all` 一致。
- 模拟加字段：`alembic revision --autogenerate` → 生成迁移 → upgrade/downgrade 可逆。
- 多机部署时第二台机器 `upgrade head` 直接对齐，不再依赖 `create_all` 副作用。

**交付**：`backend/alembic/` 全量 + `main.py` 启动调整 + `deploy/install-ubuntu.sh` 补 `alembic upgrade`。

---

### 2. JWT 迁移到 HttpOnly Cookie（P0）

**目标**：消除"XSS 即丢 token"的链路。

**现状**：
- `backend/app/core/deps.py` `get_current_account` 从 `Authorization: Bearer` 读 token。
- 前端 `frontend/src/auth.js` 把 token 写 `localStorage`，axios 带头。
- `优化.md` 7b 已评估但"本期不迁移"——现网已全站 HTTPS（Nginx + Cloudflare），迁移条件已具备。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 后端签发 | 登录成功后 `Set-Cookie: token=...; HttpOnly; Secure; SameSite=Lax; Path=/` | `api/auth.py` 登录接口 |
| 后端读取 | `get_current_account` 优先读 cookie，回落 Bearer（过渡期） | `core/deps.py` |
| 登出 | `Set-Cookie: token=; Max-Age=0` | `api/auth.py` 登出接口 |
| CSRF | SameSite=Lax 覆盖大部分；写接口加 `X-Requested-With` 校验或双提交 cookie | 中间件 |
| 前端改造 | `auth.js` 不再写 localStorage；axios `withCredentials: true`；移除手动 `Authorization` 头 | `frontend/src/auth.js`、`api.js` |
| 动态券码 | 商家扫码走同源 cookie，不受影响 | 无改动 |
| 过渡兼容 | 保留 Bearer 读取 1–2 个版本，避免小程序二期对接断裂 | `deps.py` |

**验收**：
- 登录后浏览器 DevTools → Application → Cookies 有 `token`（HttpOnly 勾选）。
- `document.cookie` 取不到 token。
- 关闭 XSS 注入点后即便有残留 XSS 也拿不到 token。
- 登出后 cookie 清空，刷新页面回到登录页。

**风险**：跨域子域（如 `api.x.com` ↔ `www.x.com`）需 `Domain=.x.com`；若前后端不同域，建议同源代理（Nginx `location /api`）。

**交付**：`deps.py` + `auth.py` + 前端 `auth.js`/`api.js` + `docs/security-ops.md` 更新 Cookie 章节。

---

### 3. 列表接口 N+1 查询修复（P0）

**目标**：列表分页接口的查询数从 O(n) 降到 O(1)。

**现状**：
- `backend/app/api/coupons.py` `coupon_to_out` 对每条 coupon 单独 `db.get(Account)` / `db.get(CouponTemplate)` / `db.get(Merchant)`。
- 列表接口（券列表、核销流水、用户列表等）若分页 50 条 → 单请求 150+ 次查询。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 券列表 | `joinedload(CouponInstance.user/template/merchant)` 一次性取 | `api/coupons.py` 列表 endpoint |
| 核销流水 | `RedemptionLog` 关联 merchant/operator/user 批量预取 | `api/coupons.py` 流水 endpoint |
| 用户列表 | `Account` + `UserProfile` + `Merchant` 一次 join | `api/users.py` |
| 批量预取 | 不适合 joinedload 的场景（如统计字段）改成 `IN` 批量 `db.query(...).filter(id.in_(ids))` 后构造 dict | service 层 |
| 验证 | 开启 `echo=True` 跑一遍列表，确认 SQL 条数 ≤ 常数 | 本地回归 |

**验收**：
- 券列表 50 条：SQL 条数从 ~150 降到 ≤ 3。
- `security_audit.py` 回归仍 0 CRITICAL/HIGH。
- 接口 P95 延迟下降（可加一次性打印对比）。

**交付**：`api/coupons.py`、`api/users.py`、`api/stats.py` 列表查询重写。

---

### 4. `database.py` engine 生命周期重构（P1）

**目标**：engine 在 `create_app()` 内按 settings 构造，支持测试切换配置。

**现状**：
- `backend/app/core/database.py` 在模块 import 时就 `engine = create_engine(url, ...)`，`url` 来自 import 时的 `get_settings()`。
- 测试想换 `DATABASE_URL`（如内存 SQLite）必须 `clear_settings_cache()` + 重建 engine，容易漏。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 延迟构造 | `engine` / `SessionLocal` 改为函数返回，或封装 `DatabaseContainer` | `core/database.py` |
| `create_app` 内装配 | `create_app()` 调 `init_engine(settings)` | `main.py` |
| 测试 fixture | `conftest.py` 提供可覆盖的 engine fixture | `backend/tests/` |
| 兼容 import | 保留 `from app.core.database import Base, get_db` 的旧路径（懒代理） | `database.py` |

**验收**：
- 测试用 `sqlite:///:memory:` fixture 跑通，不污染 `data/app.db`。
- 生产启动行为不变。

**交付**：`core/database.py` + `main.py` + `tests/conftest.py`。

---

### 5. `on_event("startup")` 迁移到 `lifespan`（P1）

**目标**：跟随 FastAPI 新生命周期 API，避免升级踩坑。

**现状**：`backend/app/main.py:110` 用 `@app.on_event("startup")`，FastAPI 已标记弃用。

**动作**：

```python
from contextlib import asynccontextmanager

@asynccontextmanager
async def lifespan(app: FastAPI):
    # startup
    assert_secure_startup(get_settings())
    reset_limiters()
    Base.metadata.create_all(bind=engine)
    ensure_schema(engine)
    ...
    yield
    # shutdown（如有）

app = FastAPI(..., lifespan=lifespan)
```

**验收**：启动日志与原行为一致；FastAPI 升级到最新版无 deprecation 警告。

**交付**：`main.py`。

---

### 6. `deploy/` 脚本归档整理（P1）

**目标**：区分"长期运维脚本"与"一次性补丁脚本"。

**现状**：`deploy/` 下 30+ 脚本，其中 `_deploy_xxx_fix.sh`、`_check_xxx.sh` 大多是历史一次性补丁（XFF 修复、issue_admin 修复、decimal 升级等），新人难以辨别。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 分类 | 长期脚本（`install-ubuntu.sh`、`remote-deploy.sh`、`harden-nginx-prod.sh` 等）保留原位 | 无改动 |
| 归档 | 一次性补丁移至 `deploy/archive/`，文件头加 `# 一次性补丁：用于 xxx，已合入主线，保留作历史参考` | `git mv` |
| README | `deploy/README.md` 加"脚本分类表"，标注每个脚本的用途与是否长期 | 更新 |
| 删除候选 | 已无意义的探针脚本（如多个 `_probe.sh` 重复变体）评估删除 | 谨慎评估 |

**验收**：`deploy/README.md` 表格完整；新人能 30 秒内找到所需脚本。

**交付**：`deploy/archive/` + `deploy/README.md`。

---

### 7. 测试覆盖率提升（P1）

**目标**：建立回归网，支撑后续重构（尤其 P0 #1/#3 的改动需要测试兜底）。

**现状**：`backend/tests/` 仅 `test_client_ip.py`、`test_security_hardening.py` 两个文件，业务逻辑几乎无测试。

**动作**：

| 模块 | 优先级 | 覆盖点 |
|------|--------|--------|
| 券生命周期 | 高 | 发券 / 作废 / 过期 / 核销 / 重复核销 |
| 权限越权 | 高 | 商家只能核销本店券；用户只能看自己的券 |
| 时长账户 | 中 | 入账 / 兑换 / 余额不足 / 并发兑换 |
| 邮箱验证码 | 中 | 发码冷却 / 过期 / 复用 / 验证后消费 |
| 动态券码 | 中 | 过期 / 篡改 / 跨用户 |
| 导出 CSV | 低 | 字段脱敏 / 行数 |

**工具**：`pytest` + `pytest-asyncio`（如需）+ `coverage`；fixture 见 P1 #4。

**验收**：核心业务路径覆盖率 ≥ 70%；CI（`docs/ci/security.yml` 或新工作流）跑测试 + 覆盖率门槛。

**交付**：`backend/tests/test_coupons.py`、`test_authz.py`、`test_points.py`、`test_email_code.py`、`test_live_code.py`。

---

### 8. Nuclei 模板进 CI（P2）

**目标**：自家渗透模板从手工工具变为回归门禁。

**现状**：`tools/nuclei-templates/` 下有 `youth-auth-401.yaml`、`youth-login-sqli.yaml`、`youth-role-idor.yaml` 三个模板，但仅手工跑。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| CI Job | staging 环境可用时 `workflow_dispatch` 跑 nuclei | `docs/ci/security.yml` |
| 本地脚本 | `scripts/run-nuclei.sh` 封装 `nuclei -t tools/nuclei-templates/ -u $TARGET` | 新增 |
| 模板维护 | 每次新增接口同步加模板；模板失效及时删 | 流程约定 |

**验收**：staging 触发 CI 后 nuclei 报告归档为 artifact；0 高危。

**交付**：`scripts/run-nuclei.sh` + `docs/ci/security.yml`。

---

### 9. 前端性能优化（P2）

**目标**：首屏体积与加载速度。

**现状**：`frontend/src/router/index.js` 未确认是否懒加载；Element Plus 全量引入；无构建产物分析。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 路由懒加载 | `() => import('./views/...')` | `router/index.js` |
| Element Plus 按需 | `unplugin-vue-components` + `unplugin-auto-import` | `vite.config.js` |
| 构建分析 | `rollup-plugin-visualizer` 跑一次看体积分布 | devDependency |
| 代码分割 | 商家扫码库 `html5-qrcode` 单独 chunk | 动态 import |

**验收**：首屏 JS gzip 体积下降 ≥ 30%；Lighthouse Performance ≥ 85。

**交付**：`frontend/vite.config.js` + `router/index.js`。

---

### 10. 结构化日志（P2）

**目标**：生产日志可聚合、可检索。

**现状**：`logging.getLogger(__name__)` 裸用，文本日志，难接 ELK / Loki。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 结构化输出 | `structlog` 或 `python-json-logger` | `core/logging.py` |
| 请求追踪 | 中间件注入 `request_id`，所有日志带上 | `main.py` 中间件 |
| 关键事件 | 登录成功/失败、核销、发券、密钥轮换等带结构化字段 | service 层 |
| 敏感字段 | 银行卡/邮箱/验证码字段统一脱敏 | `sanitize.py` 扩展 |

**验收**：生产日志为 JSON Lines；`grep request_id=xxx` 可串起单次请求全链路。

**交付**：`core/logging.py` + `main.py` + 关键 service 调用点。

---

## 实施阶段建议

```text
第 1 阶段（P0，上线后立即）—— 问题 1 + 2 + 3
  · Alembic 迁移、HttpOnly Cookie、N+1 修复
  · 必须有 P1 #7 的测试兜底（至少券生命周期 + 越权）

第 2 阶段（P1，1–2 周内）—— 问题 4 + 5 + 6 + 7
  · engine 重构、lifespan、部署脚本归档、测试网补齐

第 3 阶段（P2，持续）—— 问题 8 + 9 + 10
  · nuclei 进 CI、前端性能、结构化日志
```

---

## 验收总表（Definition of Done）

- [ ] `alembic upgrade head` 可在新机器对齐 schema；downgrade 可逆
- [ ] 登录后 token 仅存在于 HttpOnly Cookie；`document.cookie` 取不到
- [ ] 券列表 50 条 SQL 条数 ≤ 3
- [ ] 测试可用内存 SQLite fixture，不污染开发库
- [ ] FastAPI 升级无 `on_event` deprecation 警告
- [ ] `deploy/README.md` 脚本分类表完整
- [ ] 核心业务测试覆盖率 ≥ 70%
- [ ] CI 可触发 nuclei 扫描并归档报告
- [ ] 首屏 JS gzip 体积下降 ≥ 30%
- [ ] 生产日志为 JSON Lines，可按 `request_id` 串联

---

## 建议不动的边界

- **不**为迁移而迁移 ORM（SQLAlchemy 2.0 用得没问题，暂不换）
- **不**引入消息队列（业务量未到，券码生成/邮件发送同步即可）
- **不**上微服务（单体足以支撑一期业务量）
- **不**引入 TypeScript 重写前端（Vue 3 + JS 可维护性尚可，二期再评估）

---

## 与 `优化.md` 的关系

| 文档 | 范围 | 状态 |
|------|------|------|
| [`优化.md`](优化.md) | 安全硬化（OpenAPI/CORS/限流/密钥/输入净化等） | 基本完成 |
| 本文 `优化1.md` | 工程债 / 性能 / 可维护性 / 可演进性 | 待办 |

两文档**互不重叠**：安全类问题继续走 `优化.md`；本文聚焦安全之外的工程优化。
若后续出现新的安全问题，应优先追加到 `优化.md` 而非本文。

---

## 关键配置/命令速查

```powershell
# Alembic
cd backend
.\.venv\Scripts\python.exe -m alembic init alembic
.\.venv\Scripts\python.exe -m alembic revision --autogenerate -m "baseline"
.\.venv\Scripts\python.exe -m alembic upgrade head

# 测试（含新业务测试）
cd backend
.\.venv\Scripts\python.exe -m pytest tests/ -v --cov=app --cov-report=term-missing

# 前端构建分析
cd frontend
npm run build -- --mode analyze
```

---

## 状态

| 阶段 | 状态 |
|------|------|
| 计划文档 | 已写入本文 |
| 第 1 阶段（P0） | 完成（#1 Alembic、#2 HttpOnly Cookie、#3 N+1 全部落地） |
| 第 2 阶段（P1） | 完成（#4 engine 重构、#5 lifespan、#6 部署脚本归档、#7 测试覆盖全部落地；51 项业务测试通过） |
| 第 3 阶段（P2） | 完成（#8 nuclei 进 CI、#9 前端按需引入、#10 结构化日志全部落地） |

*制定日期：2026-07-31；基于全项目遍历结论。*
