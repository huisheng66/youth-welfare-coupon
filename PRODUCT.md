# Product

## Register
product

## Purpose
为特定身份青年提供身份核验、指定商家优惠券发放/兑换与到店核销的运营工具。管理端负责审核与发券，用户端领券与出示动态码，商家端核销对账。

## Users
- 平台/发券管理员：审核、发券、时长入账、查流水
- 青年用户：资料核验、看券、时长兑换、出示动态码
- 商家核销员：预览并核销本店券、导出记录

## Primary tasks
- 审核待办并尽快处理
- 给已通过用户发券或入账时长
- 用户到店出示可核销动态码
- 商家一次成功核销、可追溯

## Personality
务实、可信、清晰。公共服务工具感，不花哨。

## Anti-references
SaaS 营销落地页、厚重玻璃拟态、装饰性大数字英雄区、暖米色「AI 默认」背景。

## Accessibility
对比度可读；关键操作用按钮文案说清结果；尊重 `prefers-reduced-motion`。

## 业务资格规则
账号启用、用户核验、门店启用、模板启用四类资格的统一行为矩阵（2026-09-06，T12；
后端实现集中在 `backend/app/services/eligibility.py`，发券/入账/兑换各入口复用同一判定）：

| 场景 | 新发券（单条/批量/名单导入） | 时长入账/扣减 | 时长兑换 | 已有券使用（出示/核销） |
| --- | --- | --- | --- | --- |
| 目标账号已停用 | 拒绝 | 拒绝 | 拒绝（停用即无法登录） | 不追加限制（账号停用即不可登录出示） |
| 资料未核验 / 驳回 / 草稿 | 拒绝 | 拒绝 | 拒绝 | 不适用 |
| 已核验后身份变更、复核中（pending） | 拒绝 | 拒绝 | 拒绝 | 已有券默认保留可用，不随复核冻结 |
| 门店已停用 | 拒绝（关联商家不可用） | 不受影响 | 拒绝；兑换目录提前过滤，用户看不到 | 停止新核销并留失败流水，历史记录保留可查 |
| 模板已停用 | 拒绝 | 不受影响 | 拒绝；目录不再展示 | 只停止新增，已发券按实例有效期照常核销 |
| 模板未标价（cost_points=0） | 可发券 | 不受影响 | 不可兑换，目录不展示 | 不受影响 |

补充约定：
- 券实例保存发放时的模板名称/描述快照，模板后续编辑不改变已发券券面；
  升级前的历史券无快照，展示回退模板当前值，不回填伪造历史。
- 时长调整必须填写说明（reason），负数调整不得使余额为负。
- 批量/名单导入逐行独立判定，单行失败不影响其他行，失败原因逐行可查。
- 名单导入统一走「预检 → 确认执行 → 逐行结果」：预检不写业务数据，执行时
  重新校验资格；用户标识按 用户名 → 邮箱 → 手机 → 学号 匹配，跨字段命中
  不同用户或学号多命中必须报告歧义，姓名不作为唯一标识；文件内同一用户
  重复行拒绝并保留首次出现，不静默重复发券/入账；执行中断可续执，只重试
  失败行且不会重复入账。
- 导入账号凭证双轨（T15）：含邮箱用户收一次性激活链接（48h 单次消费）自行
  设密；无邮箱/关闭通知用户领取一次性个人凭证（仅执行结果显示一次，不入
  库不进日志）；待激活账号占位密码随机不可知，不存在默认弱口令。
- 发券/时长/兑换/核销写操作支持幂等键（T13/T16）：同 key 重试重放原结果，
  响应丢失不重复执行；商家核销超时进入结果确认流程，不把"已使用"当作
  本次成功。
