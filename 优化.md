# 安全优化计划

基于本地 API 安全自检（SQL 注入、鉴权、越权、JWT、敏感信息等）结论，对以下 **7 个问题** 做分优先级优化。  
自动化脚本：`backend/scripts/security_audit.py`、`backend/scripts/security_audit_extra.py`。  
回归测试：`backend/tests/test_security_hardening.py`。

---

## 总览

| # | 问题 | 风险 | 建议优先级 | 预估工作量 | 状态 |
|---|------|------|------------|------------|------|
| 1 | 生产暴露 `/docs`、`/openapi.json` | 信息泄露、攻击面扩大 | **P0** | 0.5 天 | **已完成** |
| 2 | 资料字段可存脚本（存储型 XSS 面） | 依赖前端转义，一旦 `v-html` 即中招 | **P0** | 1 天 | **已完成** |
| 3 | 演示弱口令 | 上线即被扫 | **P0** | 0.5 天 | **已完成** |
| 4 | 登录限流仅进程内存 | 多 worker/多机失效 | **P1** | 1–1.5 天 | **已完成** |
| 5 | CORS 过宽（含局域网正则） | 非预期源跨域调 API | **P1** | 0.5 天 | **已完成** |
| 6 | `SECRET_KEY` 兼 JWT + 卡加密 | 泄露/轮换影响面大 | **P1** | 1 天 | **已完成** |
| 7 | 未覆盖：CSP/CSRF/依赖 CVE/DoS | 纵深不足 | **P2** | 2–3 天 | **部分完成**（7a/7d 已做） |

**原则**：先关暴露面与弱口令 → 再硬化配置与限流 → 最后补扫描与前端安全头。

**当前基线**（实施后）：

- CRITICAL / HIGH：**0**（`security_audit.py` 回归）
- 登录与搜索参数：**未见可利用 SQL 注入**
- 鉴权 / 角色越权 / 银行卡解密接口：抽样 **通过**
- 登录失败：**429 限流**（memory / file / Redis 可配置）
- 安全响应头：`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`

---

## 分项计划

### 1. 关闭生产 OpenAPI 文档（P0）✅

**目标**：生产环境不可匿名访问接口文档。

| 动作 | 说明 | 实现 |
|------|------|------|
| 配置开关 | `OPENAPI_ENABLED`（默认：开发 true / 生产 false） | `app/core/config.py` |
| 应用层 | `FastAPI(docs_url=..., redoc_url=..., openapi_url=...)` 按开关置 `None` | `app/main.py` `create_app()` |
| Nginx | `/docs` `/redoc` `/openapi.json` → 404 | `deploy/nginx-welfare.conf` |
| 验收 | 生产访问 `/docs` → 404；开发仍可开文档 | 测试已覆盖 |

**交付**：`config` + `main.py` + `deploy/nginx-welfare.conf` + `.env.example`

---

### 2. 收紧用户可控文本，降低 XSS 面（P0）✅

**目标**：恶意脚本无法有效进入展示链路。

| 动作 | 说明 | 实现 |
|------|------|------|
| 服务端清洗 | `real_name` / `organization` / `remark` / `display_name` / `material_note` 等去控制字符与 `<>` | `app/services/sanitize.py` + schemas |
| 长度与字符集 | 姓名/学号剥离 `<>`；备注同样剥离标签 | `schemas/user.py` |
| 前端约定 | 全局禁止对用户字段使用 `v-html`；审计现有 Vue | `frontend/src` 无 `v-html` |
| 密码策略 | 注册/改密/重置最少 **8** 位 | schemas + 前端提示 |

**验收**：PUT 资料写入 `<script>alert(1)</script>` → 存库为净化文本；页面不执行脚本。

**交付**：`sanitize` + Profile/核验 schemas + 前端口令提示

---

### 3. 演示弱口令与上线口令策略（P0）✅

**目标**：生产不可沿用 `admin123` 等演示账密。

| 动作 | 说明 | 实现 |
|------|------|------|
| 环境区分 | `SEED_DEMO_ACCOUNTS`：开发默认真，生产默认假 | `seed.py` + config |
| 密码策略 | 最少 8 位 | schemas |
| 启动检查 | 生产默认/弱 `SECRET_KEY` → **拒绝启动** | `assert_secure_startup` |
| 运维文档 | 上线后立刻改密、关 seed | `deploy/README.md` |

**验收**：生产 `.env` 下无默认 `admin/admin123` seed（`SEED_DEMO_ACCOUNTS=false`）。

---

### 4. 登录限流可跨进程（P1）✅

**目标**：限流在多 worker / 重启后仍有效（至少单机可靠）。

| 方案 | 适用 | 说明 |
|------|------|------|
| **A. 文件/SQLite 计数** | 单机小部署 | `FileRateLimiter`（默认生产 auto） |
| **B. Redis** | 生产多机 | `REDIS_URL` + `RATE_LIMIT_BACKEND=redis` |
| **C. Memory** | 开发 | 默认开发 auto |

**实现路径**：

1. 抽象 `RateLimiter`（`check` / `hit` / `clear`）— `app/services/rate_limit.py`
2. 默认 `MemoryRateLimiter`（开发）
3. 配置 `REDIS_URL` 时用 `RedisRateLimiter`（可选 `pip install redis`）
4. 限流维度：IP + 登录标识；可选全局限流 `GLOBAL_IP_MAX_REQUESTS`
5. 响应头：`Retry-After`

**验收**：File 后端两实例共享计数仍 429；合法登录后计数清零（测试覆盖）。

---

### 5. CORS 收紧（P1）✅

**目标**：仅允许配置的正式前端源。

| 动作 | 说明 | 实现 |
|------|------|------|
| 开发 | 保留 localhost + 可选局域网（`CORS_ALLOW_LAN=true`） | 默认开发开 |
| 生产 | `CORS_ALLOW_LAN=false`，仅 `CORS_ORIGINS` | 默认生产关 |
| 代码 | `allow_origin_regex` 按开关启用 | `main.py` |
| 文档 | `.env.example` 分开发/生产示例 | 已写 |

**验收**：`Origin: https://evil.com` 无 CORS 放行头（测试覆盖）。

---

### 6. `SECRET_KEY` 与字段加密密钥分离（P1）✅

**目标**：JWT 与银行卡加密职责分离，轮换可控。

| 动作 | 说明 | 实现 |
|------|------|------|
| 新配置 | `FIELD_ENCRYPTION_KEY`；无则回退 `SECRET_KEY` + DeprecationWarning | `crypto.py` |
| JWT | 仍用 `SECRET_KEY` | `security.py` |
| 轮换 | `FIELD_ENCRYPTION_KEY_PREVIOUS` + MultiFernet 解密旧密文 | 已支持 |
| 部署 | `install-ubuntu.sh` 生成双随机密钥 | 已写 |
| 启动 | 生产禁止默认 secret | `assert_secure_startup` |

**验收**：只改 JWT secret 不影响已绑卡解密（测试覆盖）。

---

### 7. 纵深：CSP / CSRF / 依赖 / DoS（P2）

| 子项 | 动作 | 状态 |
|------|------|------|
| **7a 安全响应头** | FastAPI 中间件 + Nginx：`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy` | **已完成** |
| **7b Token 存储说明** | JWT in `localStorage` 风险 + HttpOnly Cookie 方案评估（本期不迁移） | **已完成** → `docs/security-ops.md` |
| **7c 依赖扫描** | `scripts/dep_audit.*` + GitHub Actions `security.yml`（pip-audit / npm audit） | **已完成** |
| **7d 轻量 DoS** | 全局限流 `GLOBAL_IP_*` + Nginx `client_max_body_size` + 登录限流 | **已完成** |
| **7e 可选渗透清单** | OWASP ZAP baseline 脚本与半年节奏；CI 可 `workflow_dispatch` | **已完成**（流程就绪；需 staging URL 实跑） |

详细运维说明：[`docs/security-ops.md`](docs/security-ops.md)。

---

## 实施阶段建议

```text
第 1 阶段（上线前必做）—— 问题 1 + 3 + 5 配置收紧  ✅
  · OPENAPI 关闭、CORS 生产白名单、演示账密/默认 SECRET 守卫

第 2 阶段（上线前强烈建议）—— 问题 2 + 6  ✅
  · 输入净化、密钥分离与 .env 生成

第 3 阶段（上线后 1–2 周）—— 问题 4 + 7a/7d  ✅
  · 可跨进程限流、安全头、全局限流

第 4 阶段（持续）—— 问题 7b/7c/7e  ✅
  · 依赖扫描进 CI、ZAP 基线脚本、Cookie 方案评估文档
```

---

## 验收总表（Definition of Done）

- [x] 生产：`/docs` 404，CORS 无局域网正则，无默认 SECRET
- [x] 演示弱口令不可用于生产 seed（或强制改密）
- [x] 用户文本字段无法以可执行 HTML 形态进入页面
- [x] 银行卡密钥可与 JWT 分离配置
- [x] 多 worker 下登录爆破仍限流（File/Redis 后端；测试用双 File 实例模拟）
- [x] Nginx/应用具备基础安全头；`security_audit.py` 回归仍 0 CRITICAL/HIGH
- [x] 依赖扫描可本地/CI 跑（`scripts/dep_audit.*` + `docs/ci/security.yml` 模板）
- [x] Token/Cookie 风险与迁移条件已文档化（`docs/security-ops.md`）
- [x] ZAP baseline 脚本与归档清单就绪（实扫依赖 staging）

---

## 建议不动的边界

- **不**为「安全测试」引入重型 WAF 作为第一依赖
- **不**在未上 HTTPS 前上 HttpOnly Cookie 方案（与现网部署节奏绑定）
- SQLite 开发环境可保留宽松 CORS/OpenAPI，**仅生产收紧**

---

## 关键配置速查

| 变量 | 开发默认 | 生产建议 |
|------|----------|----------|
| `APP_ENV` | `development` | `production` |
| `OPENAPI_ENABLED` | true | false |
| `CORS_ALLOW_LAN` | true | false |
| `SEED_DEMO_ACCOUNTS` | true | false |
| `SECRET_KEY` | 本地占位 | `openssl rand -hex 32` |
| `FIELD_ENCRYPTION_KEY` | 可空（回退） | 独立随机串并备份 |
| `RATE_LIMIT_BACKEND` | auto→memory | auto→file 或 redis |

---

## 回归命令

```powershell
# 单元 / 集成（不依赖外部服务）
cd backend
.\.venv\Scripts\python.exe tests\test_security_hardening.py

# 依赖 CVE（仓库根目录）
.\scripts\dep_audit.ps1

# API 已启动时
.\.venv\Scripts\python.exe scripts\security_audit.py
.\.venv\Scripts\python.exe scripts\security_audit_extra.py

# staging 上 ZAP（需 Docker + TARGET）
# .\scripts\zap-baseline.ps1 -Target https://staging.example.com
```

---

## 状态

| 阶段 | 状态 |
|------|------|
| 计划文档 | 已写入本文 |
| 第 1 阶段实施 | **已完成** |
| 第 2 阶段实施 | **已完成** |
| 第 3 阶段实施 | **已完成**（限流 + 安全头 + 轻量 IP 限流） |
| 第 4 阶段 | **已完成**（依赖扫描 CI / ZAP 流程 / Cookie 评估文档） |

*P0–P3：2026-07-28；第 4 阶段：2026-07-28。自动化验收见 `backend/tests/test_security_hardening.py`。*
