# 工程优化计划（优化2）

继 [`优化.md`](优化.md)（安全硬化）与 [`优化1.md`](优化1.md)（工程债/性能/可维护性/可演进性 P0–P2）之后，本文聚焦**上线准备、可观测性、运维自动化、业务演进**四个维度，按优先级排出可落地的优化项。

- 安全类问题继续走 `优化.md`；本文不重复。
- 与 `优化1.md` 互补：`优化1.md` 解决"代码层面的债"，本文解决"上线与运维层面的债"。

自动化回归：`backend/tests/`（78 项）、`backend/scripts/security_audit*.py`、`docs/ci/security.yml`。

---

## 总览

| # | 问题 | 维度 | 风险 | 建议优先级 | 预估工作量 | 状态 |
|---|------|------|------|------------|------------|------|
| 1 | 无 Docker 化，部署靠脚本+systemd | 上线/运维 | 环境不一致、回滚慢、横向扩展难 | **P0** | 1–1.5 天 | 完成 |
| 2 | 无错误追踪（Sentry/自建） | 可观测性 | 生产异常靠用户上报，排障滞后 | **P0** | 0.5–1 天 | 待办 |
| 3 | 无数据库自动备份 | 灾备 | 数据丢失不可恢复 | **P0** | 0.5 天 | 待办 |
| 4 | Redis 缓存层未启用（限流/会话/热点） | 性能/可扩展 | 多 worker 限流失效、热点查询重复落库 | **P1** | 1–1.5 天 | 待办 |
| 5 | 无 Prometheus 指标 / 慢查询监控 | 可观测性 | QPS/延迟/错误率盲区，慢 SQL 不可见 | **P1** | 1 天 | 待办 |
| 6 | CSP 头未真正下发（仅有 X-Frame-Options 等） | 安全 | XSS 注入后仍可执行外部脚本 | **P1** | 0.5 天 | 待办 |
| 7 | CI 无前端测试 / E2E | 质量 | 前端回归无网，重构即踩坑 | **P1** | 1.5–2 天 | 待办 |
| 8 | 统计报表单薄（仅 dashboard） | 业务/运营 | 运营决策缺数据支撑 | **P2** | 2–3 天 | 待办 |
| 9 | 商家入驻走超级管理员手工建账号 | 业务 | 入驻流程重，无法规模化 | **P2** | 2–3 天 | 待办 |
| 10 | 前端无 TypeScript，无组件测试 | 可维护性 | 重构成本高，类型错误运行时才暴露 | **P2** | 评估 | 待办 |

**原则**：先把"能上线、上线后能发现问题和回滚"的三块补齐（Docker / 错误追踪 / 备份）→ 再补性能与可观测性（Redis / 指标 / CSP / 前端测试）→ 最后做业务演进与工程升级。

---

## 分项计划

### 1. Docker 化部署（P0）

**目标**：以容器替代裸机脚本部署，统一环境、加速回滚、为水平扩展铺路。

**现状**：
- `deploy/install-ubuntu.sh` 装系统依赖 + venv + systemd service。
- `deploy/welfare-api.service` 直接跑 `uvicorn app.main:app`。
- `deploy/nginx-welfare.conf` 反代 + 静态文件。
- 环境差异（Python 版本、系统库版本）曾导致部署返工。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 后端镜像 | `backend/Dockerfile`：python:3.12-slim + requirements + 非root用户 | 多阶段构建 |
| 前端镜像 | `frontend/Dockerfile`：node:20 build → nginx:alpine serve | 多阶段构建 |
| 编排 | `docker-compose.yml`：api + web + redis + （可选 mysql） | 一键起停 |
| 健康检查 | Dockerfile `HEALTHCHECK` 命中 `/api/health` | 容器编排层 |
| 环境配置 | `.env.docker.example` 列出容器环境变量 | 配置外置 |
| 保留脚本 | `deploy/install-ubuntu.sh` 仍可用（兼容不装 Docker 的环境） | 不删 |

**验收**：
- `docker compose up -d` 后访问前端 + API 正常。
- `docker compose down && docker compose up -d --build` 完成回滚演练 ≤ 2 分钟。
- 容器以非 root 运行；`docker scan` 无 CRITICAL。

**交付**：`backend/Dockerfile`、`frontend/Dockerfile`、`docker-compose.yml`、`.env.docker.example`、`docs/deploy-docker.md`。

---

### 2. 错误追踪接入（P0）

**目标**：生产异常 5 分钟内可见，带堆栈与请求上下文。

**现状**：结构化日志已落地（`core/logging.py`），但异常靠 `logger.exception` + 日志聚合，无主动告警；未捕获异常会进 uvicorn stderr 但无聚合。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| SDK 选型 | 优先 Sentry（云托管，免费额度够用）；自建可选 GlitchTip | `sentry-sdk` |
| 初始化 | `main.py` `create_app()` 内按 `SENTRY_DSN` 初始化 | 启动钩子 |
| 采样 | 生产 100% 错误 + 1% 正常事务 | SDK 配置 |
| 上下文 | `request_id`、`user_id`、`role` 注入 scope | 中间件 |
| 个人信息 | 关闭 `send_default_pii`，银行卡/验证码字段不进 Sentry | SDK 配置 |
| 告警 | 邮件/飞书 webhook 接入（错误率突增告警） | Sentry 项目配置 |

**验收**：
- 制造一个 500 错误，Sentry 5 分钟内出现事件，含 `request_id`、堆栈、用户角色。
- 关闭 `send_default_pii` 后，事件中不含银行卡号、密码原文。
- 本地无 `SENTRY_DSN` 时 SDK 不初始化，行为与现状一致。

**交付**：`backend/app/core/observability.py`、`main.py` 初始化、`.env.example` 加 `SENTRY_DSN`、`docs/security-ops.md` 告警章节。

---

### 3. 数据库自动备份（P0）

**目标**：每日自动备份 + 异地存储 + 恢复演练。

**现状**：无任何备份机制；SQLite 文件在 `data/app.db`，MySQL 在生产机本地。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 备份脚本 | `deploy/backup-db.sh`：SQLite 用 `.backup`，MySQL 用 `mysqldump` | 脚本 |
| 定时 | crontab 每天 03:00 执行，保留近 7/30/90 天 | cron |
| 异地 | 备份文件上传至对象存储（OSS/S3/七牛）或另一台服务器 | 脚本 + rclone |
| 加密 | 备份文件用 GPG 对称加密，密钥单独管理 | 脚本 |
| 恢复演练 | `deploy/restore-db.sh` + 文档；每季度演练一次 | 脚本 + 流程 |
| 健康检查 | 备份失败发飞书/邮件告警 | 脚本 |

**验收**：
- 手动跑 `backup-db.sh` 生成加密备份文件，大小 > 0。
- `restore-db.sh` 在新机器恢复后，能登录、看券、查流水。
- 备份失败时告警通道有通知。

**交付**：`deploy/backup-db.sh`、`deploy/restore-db.sh`、`docs/backup-restore.md`、crontab 配置示例。

---

### 4. Redis 缓存层启用（P1）

**目标**：多 worker 共享限流、热点数据缓存、会话可水平扩展。

**现状**：
- `rate_limit.py` 已实现 `RedisRateLimiter`，但 `REDIS_URL` 未配置，生产用 `FileRateLimiter`（单机共享）。
- 多 worker 部署时 `MemoryRateLimiter` 失效，`FileRateLimiter` 跨机不可用。
- 热点查询（如 dashboard 统计）每次落库。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| Redis 部署 | docker-compose 加 redis 服务，或独立 Redis 实例 | 编排 |
| 限流切换 | `.env` 配 `REDIS_URL` + `RATE_LIMIT_BACKEND=redis` | 配置 |
| 缓存层 | `app/core/cache.py` 封装 Redis 客户端，提供 `get/set/delete` 与 `cached` 装饰器 | 新模块 |
| 热点缓存 | dashboard 统计、SMTP 状态、商家列表等读多写少的数据加缓存（TTL 60–300s） | service 层 |
| 缓存失效 | 写操作后主动 `delete` 对应 key | service 层 |
| 降级 | Redis 不可用时 fallback 到无缓存（不阻塞请求） | `cache.py` |

**验收**：
- 启动 2 个 worker，A worker 登录失败 5 次，B worker 检查同一账号触发 429。
- dashboard 接口 P95 延迟下降 ≥ 50%（缓存命中时）。
- Redis 关停后接口仍可用（降级到直接查库）。

**交付**：`backend/app/core/cache.py`、`docker-compose.yml` 加 redis、`.env.example`、`stats.py`/`merchants.py` 缓存接入。

---

### 5. Prometheus 指标 + 慢查询监控（P1）

**目标**：QPS/延迟/错误率/连接池可视化，慢 SQL 可追踪。

**现状**：仅有结构化日志，无指标聚合；排障靠 grep 日志。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 指标暴露 | `prometheus-fastapi-instrumentator` 暴露 `/metrics` | 中间件 |
| 业务指标 | 发券数、核销数、登录成功/失败数等自定义 Counter | service 层 |
| 慢查询 | SQLAlchemy `before_cursor_execute`/`after_cursor_execute` 记录耗时 > 100ms | event listener |
| 慢查询日志 | 慢 SQL 写入结构化日志，带 `request_id` | `core/logging.py` |
| Grafana | docker-compose 加 prometheus + grafana，预置 dashboard | 编排 + JSON |

**验收**：
- `/metrics` 返回 Prometheus 格式指标。
- Grafana 看到 QPS、P95 延迟、错误率、连接池使用率。
- 制造一个慢查询（`SELECT ... SLEEP(0.2)`），日志中出现 `slow_query` 标记。

**交付**：`backend/app/core/metrics.py`、`main.py` 中间件、`docker-compose.yml` 加 prometheus/grafana、`docs/observability.md`。

---

### 6. CSP 头硬化（P1）

**目标**：真正下发 `Content-Security-Policy`，阻断 XSS 注入后的外部脚本加载。

**现状**：
- `main.py` `apply_security_headers` 只设了 `X-Content-Type-Options` / `X-Frame-Options` / `Referrer-Policy` / `X-XSS-Protection`。
- 缺 CSP，XSS 注入后仍可加载外部脚本。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 后端 CSP | `apply_security_headers` 加 `Content-Security-Policy` 头 | `main.py` |
| 策略 | `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'` | 配置 |
| nonce | 动态生成 nonce 注入到前端模板（如需内联脚本） | 模板渲染 |
| Report-Only | 先用 `Content-Security-Policy-Report-Only` 观察一周再切硬策略 | 两阶段 |
| 前端适配 | 移除内联 `on*` 事件、`javascript:` 链接 | 前端排查 |

**验收**：
- 响应头含 `Content-Security-Policy`。
- DevTools Console 无 CSP 违规报告。
- 注入 `<script src="https://evil.com/x.js"></script>` 被浏览器拦截。

**交付**：`main.py` CSP 头、前端内联脚本排查、`docs/security-ops.md` CSP 章节。

---

### 7. CI 前端测试 + E2E（P1）

**目标**：前端重构有回归网，关键流程 E2E 自动化。

**现状**：`docs/ci/security.yml` 只有后端测试 + pip-audit + npm-audit + nuclei/zap（手动）。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 单元测试 | Vitest + @vue/test-utils，覆盖组件 props/事件/渲染 | `frontend/src/**/*.spec.js` |
| 关键组件 | StatusTag、QrCode、EmptyState、Redeem 表单等 | 优先 |
| E2E | Playwright 跑登录、发券、核销、积分兑换关键流程 | `frontend/e2e/*.spec.js` |
| CI 集成 | `docs/ci/security.yml` 加 `frontend-tests` 和 `e2e` job | workflow |
| 覆盖率 | Vitest 覆盖率门槛 ≥ 60%（前端首次） | 配置 |

**验收**：
- `npm run test` 通过所有单元测试。
- `npm run test:e2e` 跑通 4 个关键流程。
- CI 在 PR 上自动跑前端测试，失败阻断合并。

**交付**：`frontend/vitest.config.js`、`frontend/playwright.config.js`、`frontend/e2e/`、`docs/ci/security.yml` 扩展。

---

### 8. 统计报表增强（P2）

**目标**：运营决策有数据支撑，支持导出与多维度筛选。

**现状**：`stats.py` 仅有 dashboard 概览，缺趋势、对比、明细。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 趋势图 | 发券/核销/积分兑换按日/周/月趋势 | `stats.py` + 前端图表 |
| 商家维度 | 各商家核销数、核销金额、活跃度排名 | `stats.py` |
| 用户画像 | 新注册、活跃、核销率、积分使用率 | `stats.py` |
| 导出 | CSV/Excel 导出，复用 `export.py` 脱敏 | `export.py` |
| 定时报告 | 每周邮件/飞书推送关键指标 | 脚本/任务 |

**验收**：
- Dashboard 看到近 30 天发券/核销趋势曲线。
- 商家维度报表可按时间/商家筛选并导出 CSV。
- 周报自动推送，含 5 个核心指标。

**交付**：`stats.py` 新接口、前端图表组件（ECharts 或 Chart.js）、`scripts/weekly-report.sh`。

---

### 9. 商家自助入驻（P2）

**目标**：商家在线申请入驻，超管审核，无需手工建账号。

**现状**：`auth.py` `create_merchant_account` 由超管手工调用，需提供用户名/密码。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 申请表 | 商家填写店铺名、联系人、电话、营业执照号 | `merchant_applications` 表 |
| 上传 | 营业执照照片上传（OSS/本地） | 文件上传接口 |
| 审核流 | 超管审核 → 通过自动建 Merchant + Account + 随机密码 → 邮件通知 | service 层 |
| 状态查询 | 商家可查申请状态 | 接口 |
| 重试 | 拒绝后可修改重新提交 | 接口 |

**验收**：
- 商家提交申请后，超管 Dashboard 看到待审。
- 审核通过后商家收到邮件，可登录并核销券。
- 拒绝后商家修改信息重新提交成功。

**交付**：`merchant_applications` 表（Alembic 迁移）、`api/merchant_apply.py`、前端申请页 + 审核页、`services/mail.py` 通知模板。

---

### 10. TypeScript 评估与渐进迁移（P2）

**目标**：评估前端类型化收益，制定渐进迁移路径，不一次性重写。

**现状**：Vue 3 + JS，无类型约束，重构时类型错误运行时暴露。

**动作**：

| 动作 | 说明 | 实现 |
|------|------|------|
| 评估 | 写一份迁移评估：收益、成本、风险、工具链 | 文档 |
| 工具链 | Vite + vue-tsc + ESLint + TypeScript 配置 | 配置 |
| 试点 | 选 1–2 个稳定模块（如 `utils/format.js`、`api.js`）改 `.ts` | 渐进 |
| JSDoc | 暂不迁移的模块补 JSDoc 类型注解 | 注释 |
| 不强求 | 评估若收益不足，可暂缓全量迁移 | 决策点 |

**验收**：
- 评估文档给出明确建议（迁/不迁/部分迁）。
- 试点模块 `vue-tsc --noEmit` 通过。
- 现有 JS 模块不被破坏。

**交付**：`docs/ts-migration-evaluation.md`、`frontend/tsconfig.json`、试点 `.ts` 文件。

---

## 实施阶段建议

```text
第 1 阶段（P0，上线前必做）—— 问题 1 + 2 + 3
  · Docker 化、错误追踪、数据库备份
  · 完成后即可放心上线

第 2 阶段（P1，上线后 1–2 周）—— 问题 4 + 5 + 6 + 7
  · Redis 缓存、Prometheus 指标、CSP 硬化、前端测试
  · 完成后可观测、可扩展、可回归

第 3 阶段（P2，持续）—— 问题 8 + 9 + 10
  · 统计报表、商家自助入驻、TypeScript 评估
  · 完成后业务可规模化、工程可演进
```

---

## 验收总表（Definition of Done）

- [x] `docker compose up -d` 一键起停；回滚演练 ≤ 2 分钟（配置就绪，待实机验证）
- [ ] 生产异常 5 分钟内出现在 Sentry，含 `request_id` 与角色
- [ ] 每日自动备份 + 异地存储；恢复演练通过
- [ ] 多 worker 共享限流；dashboard P95 下降 ≥ 50%
- [ ] Grafana 看到 QPS/P95/错误率/连接池；慢 SQL 入日志
- [ ] 响应头含 CSP；DevTools 无违规；外部脚本被拦截
- [ ] CI 跑前端单元 + E2E；覆盖率 ≥ 60%
- [ ] Dashboard 有趋势曲线；商家维度报表可导出
- [ ] 商家可在线申请入驻；审核通过自动建账号
- [ ] TypeScript 评估文档完成；试点模块类型检查通过

---

## 建议不动的边界（延续 优化1.md）

- **不**引入消息队列（业务量未到，邮件发送同步即可；如未来需异步任务再评估 Celery/RQ）
- **不**上微服务（单体足以支撑一期业务量；Docker 化后横向扩展已够用）
- **不**一次性 TypeScript 全量重写（先评估，渐进迁移）
- **不**引入 Kubernetes（单机 Docker Compose 足够；业务量到百万级再评估）
- **不**自建 Sentry/Grafana（优先用云服务；自建运维成本高）

---

## 与已有文档的关系

| 文档 | 范围 | 状态 |
|------|------|------|
| [`优化.md`](优化.md) | 安全硬化（OpenAPI/CORS/限流/密钥/输入净化等） | 基本完成 |
| [`优化1.md`](优化1.md) | 工程债 / 性能 / 可维护性 / 可演进性（P0–P2） | 完成 |
| 本文 `优化2.md` | 上线准备 / 可观测性 / 运维自动化 / 业务演进 | 待办 |

三文档**互不重叠**：安全类走 `优化.md`；代码层工程债走 `优化1.md`；上线与运维层走本文。若后续出现新的安全或代码债问题，应优先追加到对应文档而非本文。

---

## 关键配置/命令速查

```powershell
# Docker 化
docker compose up -d --build
docker compose down
docker compose logs -f api

# 备份
bash deploy/backup-db.sh
bash deploy/restore-db.sh <backup-file>

# Redis 缓存
redis-cli -u $REDIS_URL ping

# Prometheus 指标
curl http://localhost:8000/metrics

# 前端测试
cd frontend
npm run test
npm run test:e2e
```

---

## 状态

| 阶段 | 状态 |
|------|------|
| 计划文档 | 已写入本文 |
| 第 1 阶段（P0） | 进行中（#1 Docker 完成；#2 Sentry、#3 备份待办） |
| 第 2 阶段（P1） | 待办（#4 Redis、#5 指标、#6 CSP、#7 前端测试） |
| 第 3 阶段（P2） | 待办（#8 报表、#9 商家入驻、#10 TS 评估） |

*制定日期：2026-07-31；基于 `优化1.md` 完成后的项目现状。*
